{
  "name": "@fable-doc/common",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist && node build-static-files.js",
    "build": "tsc --build"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
